There is no need to provide any additional default values or terminal commands because this project is implemented on Google Colab. This.ipynb file can be opened with Google Colab or Jupyter Notebook by simply running it. One important thing is that in order to correctly read the required CSV files, the user must have the files added to the project folder. This is because the required CSV files are read straight from the project structure.
